# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=hockey_hub_password
DB_NAME=hockeyhub_medical

# Service Configuration
MEDICAL_SERVICE_PORT=3005
NODE_ENV=development
LOG_LEVEL=debug

# JWT Secrets (If needed by this service for auth checks, otherwise optional)
# Replace with strong, unique secrets if used
# JWT_SECRET=replace_with_strong_secret_medical
# JWT_REFRESH_SECRET=replace_with_strong_refresh_secret_medical
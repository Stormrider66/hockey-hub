import { Router } from 'express';

// Returns an empty router instance. Useful for unit/integration tests that
// don't need feature routes and want to avoid complex dependencies.
export default Router(); 
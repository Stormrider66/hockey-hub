{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    // "types": ["node"] // Commented out to allow auto-discovery of all @types packages
  },
  "include": ["src/**/*", "../../types/**/*"],
  "exclude": ["node_modules", "dist"]
}

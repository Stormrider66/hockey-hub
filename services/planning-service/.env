# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=hockey_hub_password
DB_NAME=hockeyhub_planning

# Service Configuration
PLANNING_SERVICE_PORT=3006
NODE_ENV=development
LOG_LEVEL=debug

# Development Mode Settings
USE_MOCK_DATA=false
BYPASS_AUTH=true

# JWT Secrets (If needed by this service for auth checks, otherwise optional)
# Replace with strong, unique secrets if used
# JWT_SECRET=replace_with_strong_secret_planning
# JWT_REFRESH_SECRET=replace_with_strong_refresh_secret_planning

# User Service URL (Needed for authzService)
USER_SERVICE_URL=http://localhost:3001/api/v1
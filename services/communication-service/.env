# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=hockey_hub_password
DB_NAME=hockeyhub_communication

# Service Configuration
COMMUNICATION_SERVICE_PORT=3002
NODE_ENV=development
LOG_LEVEL=debug

# JWT Secrets (Required for Socket.IO authentication)
# Replace with strong, unique secrets for this service
JWT_SECRET=replace_with_strong_secret_communication
JWT_REFRESH_SECRET=replace_with_strong_refresh_secret_communication

# Optional: Add other service-specific configs if needed (e.g., Redis URL for Socket.IO adapter)
# REDIS_URL=redis://localhost:6379
name: Communication Service CI/CD

on:
  push:
    paths:
      - 'services/communication-service/**'
      - '.github/workflows/communication-service.yml'
  pull_request:
    paths:
      - 'services/communication-service/**'

jobs:
  test:
    name: Test Communication Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml
          
      - name: Enable corepack & install pnpm
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --filter "communication-service..."
        
      - name: Run linting
        run: pnpm --filter "communication-service" run lint
        
      - name: Run unit tests
        run: pnpm --filter "communication-service" run test:unit
        
      - name: Run WebSocket integration tests
        run: pnpm --filter "communication-service" run test:socket
        
      - name: Test file attachment handling
        run: pnpm --filter "communication-service" run test:file-uploads
        
  build:
    name: Build Communication Service
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/heads/release/'))
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Login to container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}-communication-service
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
        
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./services/communication-service
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
